# Publication verification

Verified 10 September 2026 using the separately served public folder.

- Source build completed with all browser imports bundled locally.
- Browser loaded all 1,057 invented records and 4,021 invented relationships.
- Links defaulted to Off; a selected demonstration record showed nine connections.
- Off hid selected connections and persisted after reloading.
- Source inspection returned only the invented demonstration content.
- The connection endpoint reported disabled and made no live provider request.
- Original 2D displayed the generated demonstration graph.
- At 390px viewport width, the document width remained 390px.
- No page errors or external browser requests occurred during these checks.
- Source/file selection excluded personal snapshots, source recovery records,
  operational logs, private provider tools, environments and credentials.
- A scan of publishable text found no machine-local user paths or common
  private-key/token signatures. All generated record IDs use the demo prefix.

The public presentation uses the same visual assets and rendering detail as
the accepted local viewer. Halo orbit checks and bounded entrance verification
are recorded in the local development work; no personal screenshots or local
benchmark records are included in this public branch.

This saves a branch; it does not update the existing Windows release, change
the other branches, or publish a hosted website.
